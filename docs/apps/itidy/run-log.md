---
title: "iTidy Run Log"
status: log
depends_on:
  - "compatibility-notes.md"
  - "../../workflows/error-driven-porting.md"
citations_used: []
---

# iTidy Run Log

Purpose: Append-only, dated record of what has actually been run against `iTidy` and observed, so a blocker that has already been diagnosed is not re-investigated from scratch.

Needed for:
- Confirming whether `compatibility-notes.md`'s prose is still current.
- Picking up work without re-deriving the current frontier from raw logs.

Notes:
- Treat this file as the durable history for `iTidy` runs. Pull forward the important facts from recent run artifacts instead of assuming those local files will still exist later. Each entry should preserve the first blocker, the useful evidence, any repo change that followed, and the next recommended step.

## Entries

### 2026-08-09 — Missing `icon.library` blocks startup past core-library setup

- Observed: repeated same-day probe runs converged on the same first app-level blocker: startup advanced past earlier launcher issues, then stopped because `icon.library` was missing during initial library setup.
- Change: none — recorded for triage.
- Next: implement `icon.library` (or the part of it `iTidy` needs first) per `../../runtime/writing-a-library-impl.md`, then rerun and add a new entry here.

### 2026-08-11 — Repo-owned `icon.library` seam advances startup to `graphics.library`

- Observed: a direct probe run now opens `icon.library` successfully and gets past the earlier missing-library failure. The next first blocker is `graphics.library`, which means the new `icon.library` override is being picked up by the in-process launcher.
- Change: added a minimal repo-owned `IconLibrary` implementation in `src/amiga_ui/vamos/icon_library.py`, registered it in `src/amiga_ui/vamos/extensions.py`, and tightened the launcher integration test so it asserts that `icon.library` opens at a non-zero address.
- Next: add the smallest honest `graphics.library` implementation needed for the next startup sequence, then rerun and record the next blocker rather than speculating ahead into wider drawing support.

### 2026-08-16 — Repo-owned stub libraries advance startup to dos.CreateDir with PROGDIR volume missing

- Observed: after implementing `icon.library`, `graphics.library`, `diskfont.library`, `workbench.library`, `gadtools.library`, and `asl.library` in a parallel fashion, the first non-library blocker was that `das.library` called `CreateDir(lock, 'PROGDIR:logs/')` which failed because the `PROGDIR:` volume name had no mapped path.
- Change: none — recorded for triage. The fix needs to either:
  1) define a `.cfg.path.auto_assigns` entry for `prog` in the launch config, or
  2) register a stub volume mapping programmatically via an extension hook before dos runs.
- Next: extend the repo's path manager extension point so it registers `PROGDIR:/sys/path/to/amiga_apps/<app>/binary/extracted` automatically when launching a probe run; rerun and record either the next missing library, structure issue, or GUI request.

### 2026-08-20 — PROGDIR volume registration fixed; GUI/window frontier reached

- Observed: `artifacts/runs/20260820T211914Z-probe-iTidy` still failed at `dos.CreateDir`/`Lock('PROGDIR:logs/')` because `PROGDIR:` was not registered when other `-V` volumes were present. After the launcher fix, `artifacts/runs/20260820T212739Z-probe-iTidy` resolves `PROGDIR:logs/` to `amiga_apps/itidy1classic/binary/extracted/logs/` and writes the app log successfully.
- Change: corrected the launcher's automatic `PROGDIR:` volume detection so other `-V` volume arguments do not suppress the `progdir:<app root>` volume. Added regression coverage for this case in `tests/test_vamos_launcher.py`.
- Next: continue from the new first blocker in the verified probe: `GetCurrentDirName` still returns the default failure value, `ENV:`/`ENVARC:` prefs paths are missing, `iffparse.library` reports `UNKNOWN(#8)`, and `intuition.library` reports `UNKNOWN(#100)`/`UNKNOWN(#85)` before the app exits with `Could not get visual info` and `Failed to open GUI window`.

### 2026-08-21 — Real IntuiMessage event bridge drives the app's `WaitPort -> GT_GetIMsg -> GT_ReplyIMsg` loop

- Observed: a scheduled host close event now becomes a real `struct IntuiMessage` posted on the main window's real `UserPort` (`0x06b330`), and the app's event loop consumes and replies to it through the sanctioned path. The full intended path `test event -> IntuiMessage -> Window.UserPort -> WaitPort -> GT_GetIMsg -> GT_ReplyIMsg` executes end-to-end with a real message on a real port. The app then `WaitPort`s the now-empty queue and fails honestly; it does **not** exit cleanly.
- Evidence:
  - NDK 3.2 `Include_H/intuition/intuition.h`: `struct IntuiMessage` (V36+) is `0x30` bytes — `ReplyMsg@0x00`, `Node@0x04`, `Class@0x10`, `Code@0x14`, `Qualifier@0x16`, `IAddress@0x18`, `MouseX@0x1C`, `MouseY@0x1E` (consecutive words, no overlap), `Seconds@0x20`, `Micros@0x24`, `IDCMPWindow@0x28`, `SpecialLink@0x2C`. IDCMP flags: `REFRESHWINDOW 0x4`, `GADGETUP 0x40`, `MENUPICK 0x100`, `CLOSEWINDOW 0x200`. The main window requests IDCMP `0x344`.
  - FD biases: `GT_GetIMsg` 72 `(a0)`, `GT_ReplyIMsg` 78 `(a1)`, `WaitPort` 384.
  - App build (`Makefile`): `vbcc +aos68k -c99 -cpu=68000 -O2 -size -DENABLE_CONSOLE`, Workbench 3.2 SDK; binary banner reads "compiled: Jan 12 2026".
  - Vamos timeline (close-event run, `-l dos:info,exec:info`): last setup call is the 2nd `SetWindowPointerA` (PC `030764`); then `WaitPort(06b330)` #1 reports the first queued message `06b358` (classic peek, left in queue); the app calls `GT_GetIMsg` twice (`06b358` then `NULL`) and `GT_ReplyIMsg` (implemented calls do not log at the enabled level — confirmed with a temporary stderr hook); then `WaitPort(06b330)` #2 finds the empty queue and raises `UnsupportedFeatureError` -> `rc=1`.
  - **Binary != repo source.** The compiled binary performs a double `WaitPort(0x06b330)` immediately after the 2nd `SetWindowPointerA` (from `safe_set_window_pointer(FALSE)` in `open_itidy_main_window`), but the checked-in source has no `WaitPort` there (just `CONSOLE_STATUS` + `return TRUE`). A `REFRESHWINDOW` probe confirmed the loop between the two `WaitPort`s is a `while ((msg = GT_GetIMsg(...))) { GT_ReplyIMsg(msg); }` **drain that discards the message without switching on `Class`** (no `GT_BeginRefresh`/`DrawBevelBoxA` refresh-handling calls appear after `WaitPort` #1, and those are the only such calls in the log). So the delivered event is consumed by the drain, not by the real event loop.
- Change: added `src/amiga_ui/vamos/event_bridge.py` (real `IntuiMessage` production + real-`UserPort` delivery + release), `PeekPortManager`/classic-peek `WaitPort` in `src/amiga_ui/vamos/exec_library.py` (empty queue -> honest `UnsupportedFeatureError`, never a fabricated message), `GT_GetIMsg`/`GT_ReplyIMsg` in `src/amiga_ui/vamos/gadtools_library.py`, the `on_window_opened` hook in `intuition_library.py`, and `event_bridge` plumbing in `launcher.py`. Added `tests/test_event_bridge.py` (layout vs NDK, `WaitPort` peek semantics, bridge scheduling/targeting/delivery/release, and an in-process delivery test). Updated `docs/platform/library-cards/intuition.library.md` to record the sanctioned real-`IntuiMessage`-on-real-`UserPort` design and the honest empty-queue `WaitPort` rule.
- Next: the remaining work is target-side, not bridge-side. (1) `graphics.library` RastPort drawing (`fix/graphics-rastport`): `Move`/`Draw`/`RectFill`/`SetAPen`/`SetBPen`/`SetDrMd` lack `ctx` and are dropped, and there is no host framebuffer. (2) Intuition text metrics (`IntuiTextLength` bias 330, `PrintIText` bias 216). (3) GadTools `DrawBevelBoxA` (observed PC `0277b2`), `GT_RefreshWindow` (observed PC `025876`), `GT_BeginRefresh`/`GT_EndRefresh`, `GT_LayoutGadgetsA` — then integration-test refresh/gadgetup events (resolve the `GadgetID` offset first: repo `0x26` vs NDK `0x22`). (4) `SetWindowPointerA` (bias 570; observed twice, PC `030740`/`030764`).

### 2026-09-06 — `graphics.library` `TextLength` measures label widths from the RastPort font

- Observed: after the RastPort drawing-state work (see the 2026-09-05 graphics session log), the probe draws all three group boxes and then hits the text/UI frontier. The **first** unimplemented call in `artifacts/runs/20260905T232639Z-probe-iTidy` was `graphics.library` `TextLength` (bias 54), logged four times as `? CALL ... -> d0=0 (default)` (counts 6/3/9 for `"Order:"`/`"By:"`/`"Position:"`, then 4 for `"Max:"` — the label-width measurements in `main_window.c` L644-L646 and the `Max:` progress label).
- Change: implemented `TextLength` in `src/amiga_ui/vamos/graphics_library.py`. It reads `RastPort.TxWidth` from 68k memory at the pre-`RasInfo` offset the target was compiled against (`RastPort+0x3C`, where `intuition_library` writes the Topaz width) and returns `count * TxWidth` for the fixed-pitch font, with a sanity-bounded fallback to the Topaz baseline (`6`) so an unpopulated/mis-offset RastPort degrades to the correct width instead of a garbage pointer. Each measurement is recorded on the host-side `RastPortState` (`record_text_length`) so a future renderer/probe can see what was measured and how wide it was told the text was. It is a real measurement, not a fake zero.
- Evidence: `tools/analyze_target_failure.py --latest` on `artifacts/runs/20260906T000103Z-probe-iTidy` no longer lists `TextLength` among defaulted calls; the four `TextLength` traps are now hooked (they no longer log at WARNING). The app still ends at the honest `WaitPort`-on-empty-queue `UnsupportedFeatureError` boundary (no regression). `tests/test_graphics_library.py` gained a `TextLength` class (7 cases: RastPort font width, a different width, unpopulated fallback, implausible-width fallback, zero-count, no-`mem` context, and measurement recording) plus a scanner guard that `TextLength` is a wired `.fd` trap; the full suite is green and `pre-commit` (ruff + pyright) passes.
- Next: the next smallest graphics-library blocker is `Text` (bias 60), the actual text **draw** call (`Text(rp, "Max:", 4)` at PC `0279ce`), which should record a text-draw op on the RastPort model (string, position, pen) rather than a no-op. After that, the Intuition text pair (`IntuiTextLength` bias 330 / `PrintIText` bias 216) and the GadTools `DrawBevelBoxA`/`GT_RefreshWindow` pair remain the drawing frontier. The RastPort `TxWidth@+0x3C` offset is now directly consumed by a returned value, so settling it against the binary (open follow-up) has higher stakes than before.

### 2026-09-06 — `graphics.library` `Text` records a text-draw op and advances the pen

- Observed: with `TextLength` implemented, the probe draws all three group boxes and then hits the remaining text/UI frontier. The **first** unimplemented graphics-library call in `artifacts/runs/20260906T175909Z-probe-iTidy` is `graphics.library` `Text` (bias 60), logged once as `? CALL Text(rp=0006a868, string=00065140, count=04) -> d0=0 (default)` at PC `0279ce` — the folder-path label draw in `main_window.c` L1860 (drawn at the pen position left by the preceding `Move`, with the truncation decision coming from `TextLength`).
- Change: implemented `Text` in `src/amiga_ui/vamos/graphics_library.py`. It is the draw counterpart of `TextLength`: it measures the drawn width as `count * RastPort.TxWidth` (same font-width read and Topaz fallback), best-effort decodes the string content from 68k memory so the op record carries *what* was drawn, records a `Text` op on the host-side `RastPortState` (draw position, string pointer, decoded content, count, width, pen) via `record_text`, and advances the pen by the drawn width per the classic contract. There is no host window yet, so this record — not a silent no-op — is what makes the call meaningful.
- Evidence: `tools/analyze_target_failure.py --latest` no longer lists `graphics.library Text` among defaulted calls; the trap is now hooked (it no longer logs at WARNING). The app still ends at the honest `WaitPort`-on-empty-queue `UnsupportedFeatureError` boundary (no regression). `tests/test_graphics_library.py` gained a `GraphicsLibraryTextTest` (6 cases: records at pen position, advances the pen, uses the RastPort font width for the advance, decodes the drawn string, zero-count does not move the pen, no-`mem` context still records) and a scanner guard that `Text` is a wired `.fd` trap; the `_FakeMem` helper is now byte-backed so both byte (text content) and word (`TxWidth`) reads work. The full suite is green and `pre-commit` (ruff + pyright) passes.
- Next: the Intuition text pair (`IntuiTextLength` bias 330 / `PrintIText` bias 216) is the natural next increment — it draws the group-box titles (the app's most visible text) — but the source builds its `IntuiText` with the **old pre-2.0 field names** (`FrontPen`/`BackPen`/`LeftEdge`/`TopEdge`/`ITextFont`/`NextText` in `main_gui.c` and `gui_groupbox.c`), which conflicts with the repo's 3.x `iT_*` layout in `gadtools_library.py`. That IntuiText byte layout must be settled against the binary before the pair can read the string pointer/font correctly. The GadTools `DrawBevelBoxA`/`GT_RefreshWindow` pair and `intuition.library` `SetWindowPointerA` also remain frontier.

### 2026-09-06 — `intuition.library` `IntuiTextLength` + `PrintIText` measure and draw the group-box titles

- Observed: with `Text` implemented, the Intuition text pair (`IntuiTextLength` bias 330 / `PrintIText` bias 216) was the natural next increment — it draws the three group-box titles (`"Folder"`, `"Tidy options"`, `"Tools"`) via `DrawGroupBoxWithLabel` in `gui_groupbox.c`. The open question from the previous entry was the **IntuiText ABI**: the source builds the struct with the old pre-2.0 field names, and the string-pointer offset is either `0x0B` (packed) or `0x0C` (GCC-aligned) depending on the compiler's alignment rule.
- **ABI settled empirically.** Rather than guess, a bounded diagnostic dumped the running target's raw IntuiText bytes. The three titles decode correctly **only at `0x0C`** (`"Folder"`/`"Tidy options"`/`"Tools"`); `0x0B` yields a short pointer and garbage. The other fields confirm the GCC-**aligned** (not packed) layout: `LeftEdge`/`TopEdge` = 0 at `0x04`/`0x06`, `ITextFont` = NULL at `0x08`, `NextText` = NULL at `0x10`. So the target's `vc +aos68k` compiler aligns members to their natural alignment. This is recorded in `docs/apps/itidy/compatibility-notes.md` (IntuiText ABI) so it is not re-derived.
- Change: implemented `IntuiTextLength` and `PrintIText` in `src/amiga_ui/vamos/intuition_library.py`, reading the string pointer (and front pen) from the confirmed aligned layout and measuring/drawing with the fixed-pitch font width (`RastPort.TxWidth` with the Topaz baseline fallback, matching `graphics.library`). `IntuiTextLength` returns `count * font width` and records the measurement (string, count, width, decoded text) on `self.itext_measures`; `PrintIText` records a `PrintIText` draw op at the explicit `(left, top)` in the IntuiText's own front pen on the host-side RastPort op log (without moving the RastPort origin, per the classic contract).
- **Shared RastPort registry.** `PrintIText` draws into the *same* window RastPort as `graphics.library` `Text`, so a future renderer needs both in one per-RastPort op log in chronological order. The launcher now creates one run-wide `RastPortRegistry` and exposes it on every library context (`ctx.rastports`); both `graphics_library` and `intuition_library` route through it (with a per-library fallback for unit tests). This is a design change in support of hosted application mode, not an iTidy special case.
- Evidence: `tools/analyze_target_failure.py --latest` on `artifacts/runs/20260906T201501Z-probe-iTidy` no longer lists the IntuiText pair among defaulted calls (both traps are now hooked, no longer logged at WARNING). The probe now draws all three group boxes with **real** measured widths (`Folder`/`Tidy options`/`Tools` group boxes calculated and drawn) and still ends at the honest `WaitPort`-on-empty-queue `UnsupportedFeatureError` boundary (no regression). `tests/test_intuition_library.py` (new) covers the pair without the binary: `IntuiTextLength` measurement (Folder/Tidy options widths, the `0x0C`-offset guard, recorded decoded text, no-`mem` → 0), `PrintIText` draw (explicit position, RastPort font width, does **not** move the pen origin, decoded text, unpopulated-RastPort fallback), the shared-registry routing (records into `ctx.rastports`, not the private fallback), and a scanner guard that both methods are valid `.fd` traps. The full suite is 92 green and `pre-commit` (ruff + pyright) passes.
- Next: the remaining `host-ui-required` frontier is the GadTools `DrawBevelBoxA` (bias 120, PC `0277b2`) / `GT_RefreshWindow` (bias 84, PC `025876`) pair — the group-box bevel the app draws right after the titles — then `intuition.library` `SetWindowPointerA` (bias 816, PC `030740`/`030764`) and `diskfont.library` `OpenDiskFont` (bias 30). `DrawBevelBoxA` takes a taglist (`VB_*` tags) and should record the bevel-box op on the shared RastPort log; it is the natural next increment.

### 2026-09-06 — `gadtools.library` `DrawBevelBoxA` + `GT_RefreshWindow` record the group-box bevel and refresh

- Observed: with the IntuiText pair implemented, the probe draws the three group-box titles and the folder-path box, then hits the group-box bevel. The `host-ui-required` frontier from `artifacts/runs/20260906T201501Z-probe-iTidy` was `gadtools.library` `DrawBevelBoxA` (bias 120, PC `0277b2`) and `GT_RefreshWindow` (bias 84, PC `025876`). The app's group boxes call `DrawBevelBox` (the varargs wrapper, `jsr -120(a6)` → `DrawBevelBoxA`) with a tag list of `GT_VisualInfo, visual_info, GTBB_Recessed, TRUE, TAG_END` (see `main_window.c` and `gui_groupbox.c`), and `GT_RefreshWindow(win, NULL)` after opening.
- Change: implemented `DrawBevelBoxA(rport, left, top, width, height, taglist)` and `GT_RefreshWindow(win, req)` in `src/amiga_ui/vamos/gadtools_library.py`. `DrawBevelBoxA` walks the tag list for `GT_VisualInfo` (`GT_TagBase+52`) and `GTBB_Recessed` (`GT_TagBase+51`) and records a `DrawBevelBox` op (explicit position/size, recessed flag, VisualInfo) on the host-side RastPort op log via a new `RastPortState.draw_bevel_box`; it draws at the explicit bounds without moving the RastPort origin. `GT_RefreshWindow` records the refresh request (window address) on the library as a host-side repaint signal for the future renderer. Neither is a no-op: both record a meaningful host-side effect, and both route the bevel op through the launcher's shared run-wide `RastPortRegistry` (`ctx.rastports`), so the window's bevel frame, `Text`, and `PrintIText` titles are one chronological per-RastPort stream.
- Evidence: `tools/analyze_target_failure.py --latest` on `artifacts/runs/20260906T211118Z-probe-iTidy` no longer lists `DrawBevelBoxA`/`GT_RefreshWindow` among defaulted calls (both traps are now hooked, no longer logged at WARNING); the remaining defaulted calls are the stateful-runtime ones (`GetSysTime`/`GetCurrentDirName`/`FreeIFF`) plus `SetWindowPointerA` and `OpenDiskFont`. The probe still ends at the honest `WaitPort`-on-empty-queue `UnsupportedFeatureError` boundary (`exec_library.py` L72, no regression). `tests/test_gadtools_bevel.py` (new, 11 cases) covers the pair without the binary: bevel-box op at the real probe bounds, recessed/VisualInfo tag parsing, no-`mem` defaults, does-not-move-the-origin, refresh-request recording (single + accumulated), shared-registry routing (records into `ctx.rastports`, not the private fallback), and a scanner guard that both methods are valid `.fd` traps (the scanner loads the repo NDK FD dir, since gadtools is not in the default set). The full suite is 103 green and `pre-commit` (ruff + pyright) passes.
- Next: the remaining `host-ui-required` frontier is `intuition.library` `SetWindowPointerA` (bias 816, PC `030740`/`030764`) and `diskfont.library` `OpenDiskFont` (bias 30). `SetWindowPointerA` moves the window (or a gadget's) on-screen position and is the natural next increment — it should record the pointer move as a host-side op rather than a no-op. The stateful-runtime calls (`GetSysTime`/`GetCurrentDirName`/`FreeIFF`) are lower priority than the visible-UI frontier.

### 2026-09-06 — `intuition.library` `SetWindowPointerA` records the busy-pointer toggle

- Observed: with the GadTools bevel/refresh pair implemented, the only remaining `host-ui-required` calls from `artifacts/runs/20260906T211118Z-probe-iTidy` were `intuition.library` `SetWindowPointerA` (bias 816, PC `030740`/`030764`) and `diskfont.library` `OpenDiskFont` (bias 30). Inspecting the app's source (`listview_columns_api.c` L3166/L3194 and `gui_utilities.c`) shows the target calls `SetWindowPointer(win, WA_BusyPointer, TRUE/FALSE, TAG_DONE)` around listview resorting — i.e. it toggles the window's **busy pointer** (the animated busy cursor), not a window move.
- Change: implemented `SetWindowPointerA(win, taglist)` in `src/amiga_ui/vamos/intuition_library.py`. It walks the tag list for `WA_BusyPointer` (and `WA_Left`/`WA_Top` for a reusable position path) and records the request (window address, busy-pointer state, any new position) on the library as a host-side op for the future renderer, rather than silently dropping it. The target's NDK encodes `TAG_USER` as the high bit, so `WA_BusyPointer = (1<<31) + 99 + 0x35 = 0x80000098` (confirmed from the app's `.i` interface, which expands `WA_BusyPointer` to exactly that).
- Evidence: `tools/analyze_target_failure.py --latest` on `artifacts/runs/20260906T213633Z-probe-iTidy` no longer lists `SetWindowPointerA` among defaulted calls (the trap is now hooked, no longer logged at WARNING); the only remaining `host-ui-required` call is `diskfont.library` `OpenDiskFont`, plus the lower-priority stateful-runtime `GetSysTime`/`GetCurrentDirName`/`FreeIFF`. The probe still ends at the honest `WaitPort`-on-empty-queue `UnsupportedFeatureError` boundary (`exec_library.py` L72, no regression). `tests/test_intuition_library.py` gained an `IntuitionSetWindowPointerATest` class (5 cases: busy TRUE, busy FALSE, new position, no-`mem` defaults, accumulation) and a scanner guard that `SetWindowPointerA` is a valid `.fd` trap. The full suite is 109 green and `pre-commit` (ruff + pyright) passes.
- Next: the only remaining `host-ui-required` call is `diskfont.library` `OpenDiskFont` (bias 30, 2x), which opens the on-disk font for a window's RastPort. It is the natural next increment — it should return a real (repo-allocated) disk-font handle and update the window's RastPort font state rather than a no-op. After that, the `host-ui-required` frontier is fully cleared and the remaining defaulted calls are the stateful-runtime ones (`GetSysTime`/`GetCurrentDirName`/`FreeIFF`), which are lower priority than the visible-UI work.

### 2026-09-06 — `diskfont.library` `OpenDiskFont` returns a font handle; the `host-ui-required` frontier is cleared

- Observed: the only remaining `host-ui-required` call from `artifacts/runs/20260906T213633Z-probe-iTidy` was `diskfont.library` `OpenDiskFont` (bias 30, 2x). The probe log confirmed the single-`a0` call (`OpenDiskFont( textAttr[a0]=… ) from PC=… -> d0=0 (default)`) — i.e. the target passes one `TextAttr*` and expects a font pointer back in `d0`. The app (`window_management.c` L382 and `IControlPrefs.c`) fills a `TextAttr` (`ta_Name`/`ta_YSize`/`ta_Style`/`ta_Flags`) and calls `OpenDiskFont(&textAttr)`, then `SetFont(rastPort, iconFont)` on success or falls back to the screen font on NULL. The NDK autodoc [S70 item OpenDiskFont] confirms `font = OpenDiskFont(textAttr)` (D0 A0), that the result is used in `SetFont`/`CloseFont`, and that `d0` is zero when the font cannot be found; the `TextAttr` layout is `ta_Name` (STRPTR) @ 0x00, `ta_YSize` (UWORD) @ 0x04, `ta_Style`/`ta_Flags` (UBYTE) @ 0x06/0x07 [S1 graphics/text.h L63-L69].
- Change: implemented `OpenDiskFont(textAttr)` in `src/amiga_ui/vamos/diskfont_library.py` (previously a stub with only `get_version`). It reads the `TextAttr` (the `ta_Name` pointer and the font-name string, plus `ta_YSize`), allocates a repo `TextFont` stand-in carrying the requested name — the same pattern the screen's default font uses — and records the request (name, size, `TextAttr` address) on the library (`self.font_opens`) so a future renderer can see which font the app asked for. It returns the stand-in handle so the app's `SetFont(rastPort, iconFont)` records a real font on the RastPort (meaningful emulated state, not an empty success or a fake disk-font load), and returns zero only when there is no 68k memory to read the `TextAttr` from, in which case the app falls back to the screen font, which it handles gracefully.
- Evidence: `tools/analyze_target_failure.py --latest` on `artifacts/runs/20260906T221414Z-probe-iTidy` no longer lists `OpenDiskFont` among defaulted calls (the trap is now hooked, no longer logged at WARNING), and **no `host-ui-required` call remains** — the visible-UI frontier is fully cleared. The remaining defaulted calls are the lower-priority stateful-runtime ones (`timer.device` `GetSysTime` 17x, `dos.library` `GetCurrentDirName` 2x, `iffparse.library` `FreeIFF` 1x). The probe still ends at the honest `WaitPort`-on-empty-queue `UnsupportedFeatureError` boundary (`exec_library.py` L72, no regression). `tests/test_diskfont_library.py` (new, 8 cases) covers the call without the binary: a non-NULL handle is returned, the requested name/size are recorded, the returned handle block carries the font name, no-`mem` returns zero, multiple opens accumulate, and a scanner guard that `OpenDiskFont` is a valid `.fd` trap (the scanner loads the repo NDK FD dir, since diskfont is not in the default set). The full suite is 117 green and `pre-commit` (ruff + pyright) passes.
- Next: the `host-ui-required` (visible-UI) frontier is fully cleared — `graphics.library` `Text`, the `intuition.library` `IntuiTextLength`/`PrintIText` pair, the `gadtools.library` `DrawBevelBoxA`/`GT_RefreshWindow` pair, `intuition.library` `SetWindowPointerA`, and `diskfont.library` `OpenDiskFont` are all implemented, gated, and merged. The remaining defaulted calls are stateful-runtime (`GetSysTime`/`GetCurrentDirName`/`FreeIFF`), which are lower priority than the visible-UI work and are the natural next frontier.

### 2026-09-08 — `iffparse.library` `AllocIFF`/`FreeIFF` handle lifecycle; the stateful-runtime frontier is cleared

- Observed: with the `host-ui-required` (visible-UI) frontier fully cleared, the only remaining defaulted calls were the stateful-runtime ones — `timer.device` `GetSysTime` (17x), `dos.library` `GetCurrentDirName` (2x), and `iffparse.library` `FreeIFF` (1x). This session cleared all three in three coherent, gated increments; `FreeIFF` was the last. `artifacts/runs/20260908T095134Z-probe-iTidy` (after `GetSysTime` and `GetCurrentDirName` were merged) showed a single iffparse defaulted call, `lib:WARNING: ? CALL: (iffparse.library) 54 UNKNOWN(#8) from PC=04b81c -> d0=0 (default)` = `FreeIFF`. Inspecting the app source (`window_management.c` L290-L347, `Settings/IControlPrefs.c` L225-L266) shows the iffparse path is `iff = AllocIFF()` → `file = Open("ENV:sys/font.prefs")` fails (the prefs file is absent in the runtime) → `FreeIFF(iff)`. The app never reaches `OpenIFF`/`ParseIFF`/`CurrentChunk` in this path, so the smallest semantic unit is an honest `AllocIFF`/`FreeIFF` handle lifecycle, not a general IFF parser.
- Change: `src/amiga_ui/vamos/iffparse_library.py` now implements a real handle lifecycle. `AllocIFF()()` allocates a tracked 256-byte block in the emulated address space (via `ctx.alloc.alloc_memory`, the same pattern as `diskfont`/`event_bridge`) and returns its address as the parser handle — a real allocation the caller may write through (e.g. `iff->iff_Stream`), not the previous constant dummy `1`. `FreeIFF(iff)` (signature fixed to take `ctx`, reading the handle from `A0`) releases exactly the handles this instance allocated (frees the underlying memory) and ignores unknown or already-freed handles as an honest no-op rather than a success on garbage. The remaining iffparse traps are left as the pre-existing no-op boundary (deliberately not a general IFF parser) and documented as such in the module docstring. Commit `53e469c` on `feat/iffparse-freiff-lifecycle`, merged ff-only into `development`.
- Evidence: `LibImplScanner` reports `AllocIFF`/`FreeIFF` as valid traps (2 valid, 0 invalid; `FreeIFF` left the 26-error boundary list). `tests/test_iffparse_library.py` (new, 9 cases) covers the lifecycle without the binary: distinct non-NULL tracked handles, real alloc addresses, no-alloc → NULL, release of a tracked handle, unknown-handle no-op, double-free frees exactly once, release-only-the-matching-handle, and a scanner guard that both are wired `.fd` traps. The full suite is 142 green and `pre-commit` (ruff check + ruff format + pyright) passes. `tools/analyze_target_failure.py --latest` on `artifacts/runs/20260908T104309Z-probe-iTidy` shows **no stateful-runtime defaulted call remaining** — `FreeIFF` (and the earlier `GetSysTime`/`GetCurrentDirName`) are gone from the defaulted list; the probe still ends at the honest `WaitPort`-on-empty-queue `UnsupportedFeatureError` boundary (`exec_library.py` L72, no regression).
- Next: the stateful-runtime frontier is fully cleared — `GetSysTime` (commit `24f6b0f`), `GetCurrentDirName` (commit `7cefab0`), and `FreeIFF` (commit `53e469c`), all merged into `development`. The only remaining analyzer items are missing prepared-runtime paths — `ENV:sys/font.prefs`, `ENV:sys/Workbench.prefs`, `ENV:sys/icontrol.prefs`, `ENVARC:sys/font.prefs`, `RAM:CRITICAL_FAILURE.log`, `S:User-Startup`, and `T` — i.e. the prepared-runtime/prefs boundary (decide whether these need prepared runtime files, assigns, or better DOS path behavior). That is a separate, lower-priority workstream, not the now-cleared stateful frontier.

### 2026-09-09 — DrawInfo ABI corrected and classic pen / RastPort draw-mode semantics; the "black bar" defect is fixed

- Observed: a real `amiga-ui run` under Xvfb rendered the iTidy main window with **group-box title backgrounds and the folder-path area as black bars**. The root cause was the fabricated `GetScreenDrawInfo` block: `dri_Pens` pointed at the screen RastPort (not a real pen-index array), so every `dri_Pens[...]` the app dereferences (`gui_groupbox.c` reads `dri_Pens[SHINEPEN]`/`[SHADOWPEN]`/`[TEXTPEN]`/`[BACKGROUNDPEN]`/`[HIGHLIGHTTEXTPEN]`) landed in garbage — effectively pen 0 = black — so the title clears and the folder-path clear were black. The target is **not** opaque about `DrawInfo`: it dereferences `dri_Pens`, `dri_Font`, and `dri_Depth`.
- Change: (1) `src/amiga_ui/vamos/intuition_library.py` now builds a genuine classic m68k `DRI_VERSION_2` `DrawInfo` (30 bytes, exact 3.x field offsets) with a **separately allocated** `dri_Pens` UWORD array holding the screen's semantic-pen→host-palette mapping; `FreeScreenDrawInfo` releases the block *and* the owned pen array without touching the screen/window RastPorts; multiple allocations have independent lifetimes. The window RastPort is initialized to the screen's pens (FgPen=`TEXTPEN`/black, BgPen=`BACKGROUNDPEN`/light grey) and **JAM1**. (2) `src/amiga_ui/host/qt_projection.py`: the host surface background is now the classic Workbench window light grey (pen 15, `0xC0C0C0`) so a `BACKGROUNDPEN` clear reads as the window surface; the draw-mode rule is scoped to **vector/fill** ops (JAM1→FgPen, JAM2→BgPen, COMPLEMENT→XOR, INVERSVID→swap) while `Text`/`PrintIText` always draw the glyphs in the FgPen foreground (JAM1/JAM2 govern the background behind the glyphs, not the glyphs) — the "JAM2 universally = BPen" rule is explicitly not applied to text.
- Evidence: local NDK (`Include_H/intuition/screens.h` `struct DrawInfo` DRI_VERSION_2 layout + semantic pen indices; `Include_H/graphics/rastport.h` JAM1/2/COMPLEMENT/INVERSVID), AutoDocs (`RectFill` "FgPen … taking into account the drawing mode", `ClearEOL` "JAM2 … to the BgPen", `InitRastPort` JAM2), and the app source (the group box does `SetAPen`+`Draw`/`RectFill` with no `SetDrMd` while `draw_folder_path_box` explicitly `SetBPen(BACKGROUNDPEN)`+`SetDrMd(JAM2)`, proving the window starts JAM1). A real offscreen run records, for the main window RastPort: group box = white shine (pen 1) / black shadow (pen 0) / JAM1 light-grey title clear (pen 15) / yellow `PrintIText` (pen 5); folder path = JAM2 light-grey clear (BgPen pen 15) + black `Text` (FgPen pen 0). New tests: `IntuitionGetScreenDrawInfoTest` (7) pins the layout, the live separate pen allocation, dereferenceable semantic pens, independent lifetimes, exact free, no-touch-screen/window, null no-op; `TextAndClearSemanticsTest` (5) pins `SetAPen(BACKGROUNDPEN)`+`RectFill`→background under JAM1 (group box) and JAM2 (folder path), `Text`-under-JAM2-uses-FgPen, a same-state fill-vs-text guard, and a **pixel-semantic** group-title-clear-equals-background assertion. Full suite 201 green (the one xvfb env-sensitive test fails only when the shell exports `QT_QPA_PLATFORM=offscreen`; passes standalone). `ruff check`/`ruff format`/`pyright` clean; `tests/run_gui_smoke_test.py` passes; `amiga-ui probe iTidy --direct` still ends at the documented `app_failed`/WaitPort boundary. Bounded `amiga-ui run iTidy` under Xvfb opens one projected window (`iTidy v1.0 - Icon Cleanup Tool`, 625x215), reaches the WaitPort boundary, and the host shell exits cleanly.
- Visible result (rendered offscreen, pixel-analyzed): window background is light grey (192,192,192, the dominant colour); the group-title clear and folder-path clear regions contain **zero black pixels** (the black bars are gone); white shine, black shadow, black text, and yellow highlight titles are all present and distinguishable from the background.
- Next: the next host-GUI increment — an interactive host event loop routing host input into real IntuiMessages, or general GadTools gadget projection — should start from a **fresh branch** off the updated `development` (this increment stays strictly within rendering correctness). The semantic-pen→host-palette mapping remains a documented approximation of the classic Workbench palette, and the window RastPort byte layout is the separately-tracked follow-up from the prior increment.

### 2026-09-09 — Documentation review qualifies drawing-semantics claims

- Observed: the corrected `DrawInfo` and semantic pen array genuinely remove the screenshot's black bars, but the visible result does not prove the renderer's general draw-mode model. The folder clear has APen and BPen set to the same grey, so it cannot discriminate them. The local header names JAM1/JAM2 as one-/two-colour modes, and the `RectFill` AutoDoc says a solid fill uses FgPen while taking the mode into account; neither directly supports selecting BPen for an entire JAM2 vector/fill operation. The renderer also paints foreground text glyphs without JAM2 background cells, approximates planar `COMPLEMENT` with RGB XOR, and ignores `IntuiText.DrawMode`.
- Change: documentation only. The graphics card now marks the whole-operation JAM mapping, inverse-video handling, RGB XOR, JAM2 text background, and `IntuiText.DrawMode` relationship as provisional or approximate. The platform/source docs now warn that the cache named `NDK3.2` contains files identifying themselves as later AmigaOS 4.1/V47 material. The central open-question register now carries these items, the inferred new-window JAM1 state, unresolved structure offsets, source/binary divergence, scanner-valid semantic no-ops, and the environment-sensitive GUI test.
- Next: proceed to static GadTools projection, but do not use the current draw-mode tests as proof of classic semantics. Resolve or refine drawing modes with a discriminating reference/source experiment when target-visible behavior depends on them. Treat scanner wiring and disappearance from the defaulted-call report as dispatch evidence only, not proof of semantic implementation.

### 2026-09-15 — Host window close: host close control → real `IDCMP_CLOSEWINDOW` → app-owned `CloseWindow`, with interactive smoke test

- Observed: after the cooperative-host-scheduler work (see `20260911T1046Z`), the main window projects and the address-based gadget path is proven, but the host window's close control was not driving the app. The app's own close path is a real `IDCMP_CLOSEWINDOW` `IntuiMessage` consumed by `WaitPort` and handled by its own `CloseWindow`. The block is a host close request that does not reach that app-owned branch.
- Change: `src/amiga_ui/vamos/event_bridge.py` gains `request_close_window` (posts one genuine `struct IntuiMessage` with `Class = IDCMP_CLOSEWINDOW`, targeting the exact window whose close control was pressed, on that window's real `UserPort`); `src/amiga_ui/host/qt_projection.py` `close_window` defers the widget (does not self-destroy) so the app's own `CloseWindow` is what removes the projection record and destroys the widget through the release path. New interactive smoke test `tests/run_interactive_close_smoke_test.py` (Xvfb-backed, real iTidy) asserts the full path from observation only: close request deferred → exactly one `IDCMP_CLOSEWINDOW` message on the real `UserPort` for the right window → `WaitPort` resumes `SATISFIED` and the message is released → the app's own `"Close gadget clicked - shutting down"` / `"Closing iTidy main window..."` banners → the app's own `CloseWindow` removes the record and the C++ widget is destroyed → clean exit 0. Merged into `development` (`cc99490`; smoke test `96df927`). See `docs/sessions/20260915T0006Z-session-log-host-window-close-completed.md`.
- Evidence: focused event-bridge + Qt-projection tests, the full non-GUI suite, the offscreen Qt suite, both interactive smoke tests (existing Exit + new Close), and `pre-commit` all green. The close is observed to be the app's own branch, not a host-side destroy.
- Next: project the Amiga menu strip — `AmigaHostWindow` already carries `has_menu_strip` but builds no host menu bar; a MENUPICK → real `IntuiMessage` route extends the address-based event path already proven twice.

### 2026-09-16 — Host menu strip: `NewMenu` strip → host `QMenuBar` → real `IDCMP_MENUPICK` → app's own `ItemAddress` walk

- Observed: the menu strip is the next address-based event path. The app builds a flat GadTools `NewMenu` template, calls `SetMenuStrip`, and on a pick walks `while (menu_number != MENUNULL) { item = ItemAddress(strip, menu_number); menu_number = item->NextSelect; }`. The block is projecting the strip and delivering a real pick the app can walk.
- Change: `src/amiga_ui/vamos/menu_state.py` (new, Qt-free) holds the classic menu ABI — `MENUNULL`/`NOMENU`/`NOITEM`/`NOSUB`, `Menu`/`MenuItem` offsets (incl. `GTMENUITEM_USERDATA` at `+0x22`), `MenuNumber` pack/decode, `resolve_menu_item_address()` (the real `NextMenu`→`FirstItem`/`NextItem`→`SubItem` walk), and the run-wide `MenuDescriptionRegistry`. `gadtools_library.py` `CreateMenusA` walks the app's flat template into real `Menu`/`MenuItem` blocks and records the host-safe description; `LayoutMenusA` fills geometry from the `VisualInfo` font metrics; `FreeMenus` releases. `intuition_library.py` `SetMenuStrip` records `Window.MenuStrip` (`+0x1C`) and projects the strip; `ItemAddress` delegates to the resolver and returns NULL rather than inventing a handle. `event_bridge.py` `menu_pick(window_addr, code)` posts a genuine `IDCMP_MENUPICK` with the packed `Code`. `qt_projection.py` builds a real non-native `QMenuBar` (submenus, separators, only selectable entries as `QtMenuAction`s carrying the Amiga addresses); activation calls the bridge. Merged into `development` (`9d294e3`; `a5cb4d0`). See `docs/sessions/20260916T0914Z-session-log-host-menu-strip-completed.md`.
- Evidence: two real defects caught by the new tests — (1) an **endless selection walk**: `ItemAddress` was called 11,710,749 times because freshly created items left `mi_NextSelect` at 0 and the app reads it zero-extended, so the loop never terminated; fixed by writing `NEXTSELECT_NULL` (`$FFFF`) with two regression tests, after which the same run ends `Closing iTidy main window... → cleanup complete`, exit 0, IMsg released. (2) a stale "menu-bar-free" assertion in `run_host_projection_smoke_test.py` inverted into a positive one (exactly one non-native `QMenuBar`, every entry an address-matching `QtMenuAction` with non-zero `amiga_item_addr` and decodable code). Full gates green.
- Next: `EasyRequestArgs` (LVO 588) — the LHA "continue/cancel" requester path — as a real blocking host dialog round trip.

### 2026-09-16 — `EasyRequestArgs` (LVO 588) → real blocking `QMessageBox` → app's own branch on the returned `LONG`

- Observed: the LHA availability check raises an `EasyRequestArgs` requester; the app branches on the returned code (continue vs. cancel). The block is translating that LVO into a real blocking host dialog whose chosen button the app actually branches on.
- Change: `src/amiga_ui/vamos/intuition_library.py` `EasyRequestArgs(ctx, window, easyStruct, idcmpPtr, args)` (Qt-free seam) decodes the app's own `struct EasyStruct` from emulated memory (`es_Title +0x08`, `es_TextFormat +0x0C`, `es_GadgetFormat +0x10`), splits the pipe-separated button list, calls the host projection's blocking dialog, and maps the clicked index to the classic result code (single → `1`, last/cancel → `0`, else 1-based position); raises `UnsupportedFeatureError` when there is no host projection (honest headless boundary). `idcmpPtr` is NULL for the target, so IDCMP-termination is out of scope. `qt_projection.py` `show_easy_request(...)` is a real parented `QMessageBox` whose last button is `RejectRole`, first `YesRole`, single `AcceptRole`, blocking on `.exec()` and returning the 0-based clicked index; the projected `CHECKBOX` became `QtGadgetCheckbox`. `event_bridge.py` toggles a `CHECKBOX`'s live checked state *before* posting `IDCMP_GADGETUP` (classic Intuition toggles the value, then reports the pick). `gadtools_library.py` `GT_GetGadgetAttrsA`/`GT_SetGadgetAttrsA` (the `A` variants the binary actually calls) read/write the live `GTCB_Checked`. Merged into `development` (`c782f23`; `73f3ef9`). See `docs/sessions/20260916T1525Z-session-log-host-easy-request-completed.md`.
- Evidence: `tests/test_easy_request_args.py` (Qt-free decode + code mapping + seam + no-projection error), `tests/test_qt_easy_request_dialog.py` (offscreen round trip), and `tests/run_interactive_lha_smoke_test.py` (`--branch continue|cancel`: enable the backup checkbox, press Start, find the real LHA `QMessageBox`, click the chosen button, read the live registry to observe the app's own branch). Full gates green.
- Next: an ASL file/directory requester (the folder picker) — `AllocAslRequest`/`AslRequest` → real `QFileDialog`.

### 2026-09-17 — ASL directory picker: `AllocAslRequest`/`AslRequest` → real `QFileDialog` (initial)

- Observed: the folder picker is the next iTidy path. The app allocates an ASL requester and calls `AslRequest`, then copies the selected drawer out. The block is providing a real `struct FileRequester` lifecycle and a real directory dialog.
- Change: `src/amiga_ui/vamos/asllibrary.py` (new) `AllocAslRequest`/`FreeAslRequest`/`AslRequestTags`/`AslRequest`; `qt_projection.py` `show_file_dialog(...)`. Merged into `development` (`560ab0f`; `4ab4e54`, `d456a3a`, `e794183`). See `docs/sessions/20260917T0303Z-session-log-host-asl-directory-requester.md`.
- Evidence: `tests.test_asl_library`, `tests.test_qt_asl_dialog`, the LHA smoke test, `run_gui_smoke_test.py`, `unittest discover`, and `pre-commit` reported green at merge time.
- Next: (none recorded here — see the 2026-09-18 entry, which re-verified this increment and found it had not actually worked end to end.)

### 2026-09-18 — ASL fix: "merged but never worked" — dispatch annotations, tag base, struct layout, owned strings, real smoke test

- Observed: re-verified from git, the merged ASL requester never drove a dialog. Root cause: the dispatched methods were type-annotated under `from __future__ import annotations`; vamos builds its register binder from `inspect.getfullargspec(method).annotations` and calls each annotation as a type constructor, so with PEP 563 the first guest call died `TypeError: 'str' object is not callable`. (This is why the repo's other libraries ship dispatch methods with **no** parameter annotations.) Additional defects: wrong tag base (`0x8000` vs. the real `TAG_USER + 0x80000` = `0x80080000`), tag-value masking that matched nothing, a fabricated `0x50` struct size (real `FileRequester` header is `0x38` with `fr_Pattern` at `0x34`), guest-memory corruption on accept (wrote a Python value into guest memory), wrong allocator signature, and a committed `build/iTidy.lha` symlink. The committed "smoke test" launched the archive file itself and only asserted `proc.poll() is None` — it never drove anything.
- Change: `asllibrary.py` rewritten — correct tag base and full flat Exec tag walk (`TAG_DONE`/`TAG_IGNORE`/`TAG_MORE`/`TAG_SKIP`), real `0x38` `FileRequester` layout zeroed at alloc, string tag values *copied* into ASL-owned memory, alloc-time options persisted so `AslRequest(freq, NULL)` sees them, accept writes `fr_Drawer`/`fr_File` owned strings and returns TRUE (cancel returns FALSE leaving the struct untouched; NULL requester returns FALSE), `FreeAslRequest` frees owned strings then the block via the real allocator signature, idempotent and double-free-safe, all dispatch signatures un-annotated. `src/amiga_ui/host/projection.py` protocol gains `show_file_dialog`; `qt_projection.py` implements a real non-native blocking `QFileDialog` (Directory mode for `DrawersOnly`, unparented when the app passes no `ASLFR_Window`). New `tests/run_interactive_asl_smoke_test.py` rewritten with real-rigor (click the projected Browse… gadget, capture the real dialog title/mode/parent, point it at a real directory, click Choose/Cancel, then observe the app's own branch — accept → the path appears in a live `Text()` op, cancel → it never does). `tests/test_asl_library.py` (24 Qt-free) + `tests/test_qt_asl_dialog.py` (8 offscreen). The committed `build/iTidy.lha` symlink removed. Merged into `development` (`badef9f`; `f530240`). See `docs/sessions/20260918T0303Z-session-log-host-asl-directory-requester-fix-completed.md`.
- Evidence: the rewritten interactive smoke test caught the dispatch crash on its first run (target exit right after the Browse click); after the fix the real round trip is observed live and the full gates are green.
- Next: a second real window with its own event loop — the "Restore Backups…" gadget (next increment).

### 2026-09-19 — "Restore Backups…" second window: real host window + its own gadgets, real `ModifyIDCMP`, real `Cancel` action → app's own `CloseWindow`

- Observed: the "Restore Backups…" button (`GID_RESTORE`, `main_window.c` L848) opens a second real window with its own event loop (`open_restore_window` → title "iTidy - Restore Backups"). The baseline probe's WaitPort-on-empty-queue boundary is expected, not the target. An exploratory run initially looked blocker-free, but that reading was wrong: a missing library method does not crash, it is **silently defaulted** (returns `d0=0`) and logged `? CALL: (intuition.library) 150 ModifyIDCMP( … ) from PC=… -> d0=0 (default)`. The analyzer (`tools/analyze_target_failure.py --latest`) exposed the real blocker: the app calls `ModifyIDCMP` twice (mask the main window to `0` while the restore window is up, then restore the `GADGETUP|MENUPICK|REFRESH|CLOSEWINDOW` set), and both calls were being defaulted to no-ops.
- Change: `src/amiga_ui/vamos/intuition_library.py` `ModifyIDCMP(self, ctx, window, flags)` (no annotations, VOID) writes the real `Window.IDCMPFlags` at offset `0x52` and updates the host bridge's per-window filter state. `src/amiga_ui/vamos/event_bridge.py` `on_window_idcmp_changed(window_addr, idcmp_flags)` updates the record so `gadget_up` / `request_close_window` admit or withhold events exactly as real Intuition filters them. New interactive smoke test `tests/run_interactive_restore_backups_smoke_test.py` (Xvfb, real iTidy, ASL-test rigor): click the real "Restore Backups…" gadget → observe the second host window open with title "iTidy - Restore Backups" and its own gadget set (Restore Run, Restore window positions, Delete Run, View Folders…, Cancel) → drive one real action inside it (the real "Cancel", the only enabled action with 0 backup runs) → observe **the app's own branch**: the restore host window disappears from `projection.windows` (the app's own `CloseWindow`), not a host-side destroy, plus a real `IDCMP_GADGETUP` posted on the restore window's `UserPort` and released, `WaitPort` `SATISFIED`, clean exit 0. (The app log is deliberately not relied on — this release binary is silent there; the observable is the projection + released IMsg.) Unit tests: `tests/test_event_bridge.py::test_modify_idcmp_mask_withholds_then_readmits_gadgetup` (mask to 0 withholds, re-set readmits) and `tests/test_intuition_library.py::IntuitionModifyIDCMPTest` (3 cases: struct write + ordered bridge notifications, no-bridge still writes the struct, null window no-op) plus the scanner guard `test_modify_idcmp_is_a_wired_trap`.
- Evidence: `unittest discover` 380 tests OK; `ruff check`/`ruff format --check` clean; `pyright` 0 errors; the restore smoke test PASS (exit 0); the existing ASL smoke test still PASS (regression); `tools/analyze_target_failure.py --latest` no longer lists `ModifyIDCMP` (remaining defaulted: only `exec.library` `RemTask` bias 288, obligation=support, and the known prepared-runtime missing paths `ENV:`/`ENVARC:`/`S:`/`RAM:` — the latter is the long-standing lower-priority prepared-runtime boundary, **not** a frontier to chase this increment); API index regenerated (`ModifyIDCMP` → `implemented: true`, valid trap). On `feat/restore-backups-tool-cache-window` (from `development` @ `badef9f`); merge into `development` pending the gates recorded in the session log.
- Next: "Fix Default Tools…" (`GID_VIEW_TOOL_CACHE`, `main_window.c` L830) → `open_tool_cache_window` (title "iTidy - Default Tool Analysis") — the sibling second-window case. If it shares this increment's second-window machinery cleanly, it is the natural next increment (do not do both in one session). Note: `GT_BeginRefresh`/`GT_EndRefresh` remain missing and are only reachable via `IDCMP_REFRESHWINDOW` (nothing posts one in this binary); record, do not implement speculatively.
